<%= form_for(@template_library) do |f| %>
  <% if @template_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@template_library.errors.count, "error") %> prohibited this template_library from being saved:</h2>

      <ul>
      <% @template_library.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.text_field :title , :placeholder=> "Title" , :class => "form-control" %>
    </div>
<% end %>
